# Server Configuration
PORT=8080

# Upload Settings
MAX_UPLOAD_SIZE_MB=10

# Rate Limiting (requests per minute per IP)
RATE_LIMIT=100

# API Key (leave empty to disable authentication)
API_KEY=secret123

# Logging
LOG_LEVEL=info

# Timeouts
SHUTDOWN_TIMEOUT=30s
REQUEST_TIMEOUT=60s

# Worker Pool Size (for concurrent row processing)
WORKER_POOL_SIZE=10
